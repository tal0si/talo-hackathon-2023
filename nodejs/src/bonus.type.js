import Enum from 'enum'

/**
 * @see https://www.npmjs.com/package/enum
 */
export default new Enum({'Prime de cooptation': 100, 'Prime de mission':200, 'Prime de remontée de mission': 150, 'Prime de dividende': 300})

